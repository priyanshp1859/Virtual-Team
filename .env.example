# Provision Postgres through Vercel Marketplace; keep credentials server-side.
DATABASE_URL=
# Generate separate random values, each at least 32 characters.
WORKSPACE_ACCESS_CODE=
SESSION_SECRET=
# Enable only after applying db/002-runtime.sql.
SAM_RUNTIME_ENABLED=false
